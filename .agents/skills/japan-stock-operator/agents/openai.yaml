interface:
  display_name: "日本株日次運用"
  short_description: "日次監視、期限レビュー、注文候補、引き継ぎを一括実行"
  default_prompt: "Use $japan-stock-operator to run today’s Japanese-stock operation, resume the private handoff state, and save the report and proposed order tickets."
